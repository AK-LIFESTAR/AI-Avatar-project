appId: com.projecta.prototype
productName: Project A Prototype
directories:
  buildResources: resources
  output: release/${version}
files:
  - 'dist/**/*'
  - 'out/**/*'
  - 'resources/**/*'
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
extraResources:
  # Bundle backend payload inside the app at: process.resourcesPath/backend
  # At runtime, the app copies this to a writable folder (app.getPath('userData')/backend)
  - from: backend-dist
    to: backend
    filter:
      - '**/*'
asarUnpack:
  - resources/**
win:
  executableName: ProjectAPrototype
  icon: resources/icon.ico
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  differentialPackage: false
  # Request admin rights to avoid permission issues
  allowElevation: true
  perMachine: false
  # Use one-click installer for simpler/faster installation
  oneClick: true
  # Run app after install
  runAfterFinish: true
mac:
  icon: resources/icon.icns
  target:
    - target: dmg
      arch: [x64, arm64]
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
  writeUpdateInfo: false
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
# Disable electron-builder's built-in publish to avoid PublishManager errors.
# We use a separate GitHub Action (softprops/action-gh-release) for releasing.
publish: null